<form method="post" action="{{ path('user_delete', {'id': listUser.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ listUser.id) }}">
	<a href="{{path('user_delete', {id: listUser.id}) }}">
		<button type="button" class="btn btn-outline-danger btn-sm">
			<a href="{{path('user_delete', {id: listUser.id}) }}">
				c
			</a>
		</button>

	</form>
